# =============================================================================
# Git Functions Module Index
# =============================================================================
# Automatically loads all git-related functions from this directory
#
# Organization:
#   aliases.fish    - Git aliases (g, addup, push, etc.)
#   branch.fish     - Branch creation (gh-start, gh-branch)
#   workflow.fish   - PR workflow (commit, compush, gh-pr, gh-finish)
#   docs.fish       - Documentation (gh-workflow)
#   labels.fish     - GitHub labels (gh-labels-init)
#   utilities.fish  - Utilities (git-echo-diff, gh_create_issues_from_file)
#   deploy.fish     - Deployment (ship, ship-prod)

# All functions are automatically loaded by Fish from this directory
